Some config-codes:

* CLT xcode: xcode-select --instal
* homebrew: mkdir homebrew && curl -L https://github.com/Homebrew/brew/tarball/master | tar xz --strip 1 -C homebrew
* zsh
* prezto: https://github.com/sorin-ionescu/prezto [TODO]
* task warrior: brew install task
* neofetch: git clone https://github.com/dylanaraps/neofetch.git

* tmux: 
      git clone https://github.com/tmux/tmux.git
      cd tmux
      sh autogen.sh
      ./configure && make
* musikcube: [TODO: set alias m as musikcube] 
  brew tap clangen/musikcube 
  brew install musikcube

--------------
#show/hide hidden file:
defaults write com.apple.Finder AppleShowAllFiles True/False
killall Finder